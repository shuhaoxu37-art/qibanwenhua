# AI 漫剧一键生成 App —— 设计方案

> 目标：输入一个故事点子 → 自动生成图片、配上提示词、拼成一条 1 分钟以内的漫剧短视频。

---

## 一、它长什么样（用户看到的界面）

```
┌─────────────────────────────────┐
│   AI 漫剧生成器                   │
├─────────────────────────────────┤
│                                  │
│  ① 输入你的故事点子               │
│  ┌───────────────────────────┐   │
│  │ 例：穷小子抽卡抽到神龙，     │   │
│  │ 逆袭成为大佬...             │   │
│  └───────────────────────────┘   │
│                                  │
│  ② 选风格  [国风▼] [赛博▼] [二次元▼]│
│                                  │
│  ③ 选时长  [30秒] [45秒] [60秒]    │
│                                  │
│      [ 🎬 一键生成 ]              │
│                                  │
├─────────────────────────────────┤
│  生成结果：                       │
│   - 分镜脚本（6-8个镜头）          │
│   - 每个镜头的图片                 │
│   - 每个镜头的提示词               │
│   - 拼好的成片视频                 │
│      [ 下载 ]  [ 重新生成某一镜 ]  │
└─────────────────────────────────┘
```

---

## 二、点一下"生成"，背后发生了什么（核心流程）

```
你的点子
   │
   ▼
【第1步】AI 写分镜脚本
   把你一句话的点子，拆成 6-8 个镜头，
   每个镜头有：画面描述 + 台词/旁白 + 时长
   （这一步用 Claude / GPT 来写）
   │
   ▼
【第2步】AI 写每个镜头的"出图提示词"
   把每个画面描述，翻译成出图工具能懂的提示词
   （包含：风格、人物、场景、镜头角度）
   │
   ▼
【第3步】调用出图工具生成图片
   把提示词发给「可灵/即梦/Midjourney」的 API
   每个镜头出 1 张图（或 1 段动态）
   │
   ▼
【第4步】（可选）图片转动态视频
   把静图发给「可灵/Runway」做成几秒钟的动态
   │
   ▼
【第5步】用 ffmpeg 自动拼接
   把所有镜头的视频/图片，按顺序拼起来
   加上转场、字幕、背景音乐
   │
   ▼
   一条 1 分钟漫剧成片 ✅
```

---

## 三、需要用到的"外部工具"（关键！）

这个 App **本身不会画图、不会生成视频**，它是个"指挥中心"，
真正干活的是下面这些工具，**必须是开放了 API 的**：

| 环节 | 工具选项 | 是否有 API | 大概费用 |
|------|---------|-----------|---------|
| 写脚本/提示词 | Claude API / GPT API | ✅ 有 | 很便宜 |
| 生成图片 | 即梦、可灵、Midjourney | ⚠️ 部分有 | 按张算 |
| 图片转视频 | 可灵、Runway、海螺 | ⚠️ 部分有 | 按秒算 |
| 拼接剪辑 | ffmpeg（免费开源） | ✅ 不需API | 免费 |
| 配音（可选）| 火山引擎、Azure TTS | ✅ 有 | 很便宜 |
| 背景音乐 | 本地素材库 | - | 免费 |

> ⚠️ **最大的卡点：出图/出视频工具的 API。**
> 国内的可灵、即梦有企业 API，但要申请、要充值。
> 这一步决定了 App 能不能真正"全自动"。

---

## 四、技术怎么搭（给懂技术的人看的）

```
前端（界面）        →  网页：HTML + 简单JS  或  小程序
后端（指挥中心）     →  Python（最适合，AI库多）
   ├─ 调 Claude API     写脚本、写提示词
   ├─ 调 出图 API        生成图片
   ├─ 调 视频 API        图转视频
   └─ 调 ffmpeg          拼接成片
存储                →  生成的图片/视频存哪（本地 或 云存储）
部署                →  云服务器（让它一直在线）
```

---

## 五、分阶段实现（从简单到完整）

### 🟢 阶段1：最小可用版（最容易，先做这个）
- 你输入点子 → AI 自动写**分镜脚本 + 每镜提示词**
- 输出一个文档，你拿着提示词**自己去可灵手动出图**
- **价值：省掉你写脚本、想提示词的时间**
- 难度：⭐ 低（不需要出图 API，今天就能做雏形）

### 🟡 阶段2：半自动版
- 自动出图（需要出图 API）
- 自动用 ffmpeg 拼接静图成视频
- 难度：⭐⭐⭐ 中（卡在出图 API 申请）

### 🔴 阶段3：全自动版
- 点子 → 成片，全程一键
- 图转动态、自动配音、自动字幕
- 难度：⭐⭐⭐⭐⭐ 高（要钱、要服务器、要调试）

---

## 六、给你的诚实建议

1. **先做阶段1**（脚本+提示词生成器），这个最实用、最便宜、马上能用。
2. 阶段2、3 真正的钱和难点，**全在"出图/出视频的 API"上**，
   不是写代码的问题，是那些工具开不开放、贵不贵的问题。
3. 别一上来就想"全自动一键成片"，那是大公司几十人团队在做的事。

---

## 下一步

如果你同意，我可以**马上动手做"阶段1"**：
一个能把你的故事点子，自动变成"分镜脚本 + 每个镜头出图提示词"的小工具。

这个不需要任何付费 API，先跑起来给你看效果。
